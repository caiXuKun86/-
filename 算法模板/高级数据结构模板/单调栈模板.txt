```c++
遍历
	for(int i=0;i<n;i++){
		while(top>0&&a[s[top-1]]>=a[i]){
			cur=s[--top];
			ansl[cur]=top>0?s[top-1]:-1;
			ansr[cur]=i;
		}
		s[top++]=i;
	}
结算
	while(top>0){
		cur=s[--top];
		ansl[cur]=top>0?s[top-1]:-1;
		ansr[cur]=-1;
	}

修正
	for(int i=n-2;i>=0;i--){
		if(ansr[i]!=-1&&a[ansr[i]]==a[i]){
			ansr[i]=ansr[ansr[i]];
		}
	}
	for(int i=0;i<n;i++){
		cout<<ansl[i]<<' '<<ansr[i]<<endl;
	}
```

