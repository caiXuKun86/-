```
构建等差数列差分数组diff
int set(int l,int r,int s,int e,int d){
	diff[l]+=s;
	diff[l+1]+=d-s;
	diff[r+1]-=d+e;
	diff[r+2]+=e;
}
两次前缀和
	for(int i=1;i<=n;i++){
		arr[i]+=arr[i-1];
	}
	for(int i=1;i<=n;i++){
		arr[i]+=arr[i-1];
	}

```

